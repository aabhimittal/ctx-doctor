# AGENTS.md

You are an expert senior TypeScript engineer working on the Acme API.

## Project overview

This project uses TypeScript, Vitest and ESLint.

The `src/` directory contains the source code. The `tests/` directory contains the tests.

```
src/
├── index.ts
├── server.ts
└── db.ts
tests/
└── index.test.ts
```

## Commands

```bash
npm run build   # build the project
npm run test    # run the tests
npm run lint    # run the linter
```

Run `npm run lint` to lint the code.

## Code style

- Write clean, maintainable code.
- Follow best practices.
- Indent with 2 spaces.
- Use a 4 space indent for JSON files.
- Always add JSDoc comments to exported functions.
- Never add comments; the code should speak for itself.
- Use pnpm for installing dependencies.
- Use npm for installing dependencies.
- Keep line length under 80 characters.
- The maximum line length is 120 characters.

## Testing

- Always write tests for new behaviour.
- Always write tests for new behaviour.
- Make sure everything works before you finish.

## Contributing

See the contribution guide in docs/CONTRIBUTING.md for the full workflow.

This project is MIT licensed.
